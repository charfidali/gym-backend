{
  "extensions": ["pdo_mysql", "intl", "iconv", "ctype"],
  "buildCommand": "composer install --ignore-platform-reqs --no-scripts",
  "predeployCommand": "sh ./run-app.sh",
  "startCommand": "php bin/console doctrine:migrations:migrate --env=prod --no-interaction || true && php -S 0.0.0.0:$PORT -t public"
}
